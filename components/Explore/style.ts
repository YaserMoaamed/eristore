import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'row'
    },
    text: {
        alignItems: 'center',
        paddingTop: 43,
        fontSize: 20,
        fontWeight: 'bold',
        color: '#181725'
    }
})

export default styles